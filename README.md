# nakh-daghestanian-assignment

Parsing Chechen-Russian dictionary data and outputting in the agreed upon format.

## Requirements

* Python
* PDM (can be installed with `python -m pip install pdm` or through your system's package manager)

## Usage

```bash
pdm install
pdm run dev
```

Outputs `processed.csv`.
